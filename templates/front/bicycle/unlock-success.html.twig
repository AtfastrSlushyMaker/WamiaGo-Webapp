{% extends 'front/base.html.twig' %}

{% block title %}Bicycle Unlocked - WamiaGo{% endblock %}

{% block page_stylesheets %}
    <style>
        .success-container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px 15px;
        }
        
        .success-icon {
            font-size: 80px;
            color: #28a745;
            text-align: center;
            margin: 20px 0;
        }
        
        .countdown {
            font-size: 18px;
            text-align: center;
            margin: 30px 0;
        }
        
        .bicycle-details {
            background-color: #f8f9fa;
            border-radius: 10px;
            padding: 20px;
            margin-top: 20px;
        }
        
        .instruction {
            margin-bottom: 10px;
            padding-left: 25px;
            position: relative;
        }
        
        .instruction i {
            position: absolute;
            left: 0;
            top: 3px;
            color: #28a745;
        }
    </style>
{% endblock %}

{% block content %}
<div class="container py-5">
    <div class="success-container">
        <div class="card border-0 shadow">
            <div class="card-body p-4 text-center">
                <div class="success-icon">
                    <i class="fas fa-check-circle"></i>
                </div>
                
                <h1 class="mb-4">Bicycle Unlocked!</h1>
                
                <p class="lead">Your bicycle has been successfully unlocked. You can now begin your ride.</p>
                
                <div class="alert alert-success my-4">
                    <p class="mb-0"><strong>Bicycle ID:</strong> #{{ bicycle.idBike }}</p>
                    <p class="mb-0"><strong>Reservation Code:</strong> {{ code }}</p>
                </div>
                
                <div class="countdown" id="timerDisplay">
                    Your ride started at <strong>{{ "now"|date('H:i') }}</strong> on <strong>{{ "now"|date('F d, Y') }}</strong>
                </div>
            </div>
        </div>
        
        <div class="bicycle-details mt-4">
            <h4>Bicycle Information</h4>
            <div class="row">
                <div class="col-md-6">
                    <p><strong>Type:</strong> {{ bicycle.batteryLevel > 90 ? 'Premium' : 'Standard' }} E-Bike</p>
                    <p><strong>Battery Level:</strong> {{ bicycle.batteryLevel }}%</p>
                    <p><strong>Estimated Range:</strong> {{ bicycle.rangeKm }} km</p>
                </div>
                <div class="col-md-6">
                    <p><strong>Station:</strong> {{ bicycle.bicycleStation.name }}</p>
                    <p><strong>Hourly Rate:</strong> {{ (bicycle.batteryLevel > 90 ? 15.000 : 10.500)|number_format(3) }} TND</p>
                </div>
            </div>
        </div>
        
        <div class="card mt-4 border-0 shadow-sm">
            <div class="card-body">
                <h4>Important Information</h4>
                <div class="instruction">
                    <i class="fas fa-info-circle"></i>
                    <p>Your ride time has started and you will be charged based on the time you use the bicycle.</p>
                </div>
                <div class="instruction">
                    <i class="fas fa-info-circle"></i>
                    <p>You can return the bicycle to any station when you're done.</p>
                </div>
                <div class="instruction">
                    <i class="fas fa-info-circle"></i>
                    <p>Make sure to properly dock the bicycle at a station to end your rental.</p>
                </div>
                <div class="instruction">
                    <i class="fas fa-info-circle"></i>
                    <p>For assistance, please call our support team at +216 71 123 456.</p>
                </div>
            </div>
        </div>
        
        <div class="mt-4 text-center">
            <a href="{{ path('app_front_my_reservations') }}" class="btn btn-primary">
    <i class="fas fa-list-alt me-2"></i> View My Reservations
</a>
            <a href="{{ path('app_front_services_bicycle') }}" class="btn btn-outline-secondary ms-2">
                <i class="fas fa-bicycle me-2"></i> Back to Bicycles
            </a>
        </div>
    </div>
</div>
{% endblock %}