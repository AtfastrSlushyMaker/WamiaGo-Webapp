<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Booking Details</title>
</head>
<body>
    <h1>Booking Details</h1>
    <p><strong>Booking ID:</strong> {{ booking.id_booking }}</p>
    <p><strong>Passenger Name:</strong> {{ booking.user.name }}</p>
    <p><strong>Trip:</strong> {{ booking.trip.departure_city }} to {{ booking.trip.arrival_city }}</p>
    <p><strong>Reserved Seats:</strong> {{ booking.reserved_seats }}</p>
    <p><strong>Status:</strong> {{ booking.status }}</p>
</body>
</html>