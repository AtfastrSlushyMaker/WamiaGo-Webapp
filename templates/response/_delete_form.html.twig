<form method="post" action="{{ path('app_response_delete', {'id_response': response.id_response}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ response.id_response) }}">
    <button class="btn">Delete</button>
</form>
