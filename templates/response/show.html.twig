<!DOCTYPE html>

<title>Response</title>

{% block body %}
    <h1>Response</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id_response</th>
                <td>{{ response.idResponse }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ response.content }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ response.date ? response.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_response_index') }}">back to list</a>

    <a href="{{ path('app_response_edit', {'id_response': response.id_response}) }}">edit</a>

    {{ include('response/_delete_form.html.twig') }}
{% endblock %}
